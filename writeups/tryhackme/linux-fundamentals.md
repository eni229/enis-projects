##Linux fundamentals part1

Linux - umbrella term for multiple OS's based on UNIX
First released in 1991

echo - repeats back to u. must use double quotes if more than one word. 
whoami = tells you who you are logged in as
ls = listing
cd = change directory
cat = concatenate
pwd = print working directory
find = find a spectific file. e.g. fine -name *.txt   --- finds all files ending in .txt
grep = searching the contents of a file. similar to find, but works in files. 

Symbols
& = run commands in background
&& combine multiple commands
> redirector - take output from a command and direct somewhere else
>> same as before, but appends instead. 

##linux fundamentals part2 - not available on free plan rippppp